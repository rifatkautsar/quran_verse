import 'package:flutter/material.dart';

class ItemSurat extends StatelessWidget {
  const ItemSurat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
